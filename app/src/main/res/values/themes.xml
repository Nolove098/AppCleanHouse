<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AppCleanHouse" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/app_primary</item>
        <item name="colorSecondary">@color/app_secondary</item>
        <item name="android:colorBackground">@color/app_background</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorError">@color/app_accent</item>
        
        <!-- Shape Overrides can be done here or via specific widget styles -->
        <item name="materialButtonStyle">@style/Widget.App.Button.Primary</item>
        <!-- Note: CardView and TextInputLayout styles usually applied directly via style attribute or theme overlay -->
    </style>

    <!-- UI Component Styles -->
    <style name="Widget.App.Button.Primary" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/app_primary</item>
        <item name="cornerRadius">50dp</item> <!-- Pill shape -->
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <style name="Widget.App.Card" parent="Widget.Material3.CardView.Elevated">
        <item name="cardBackgroundColor">@color/white</item>
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">4dp</item>
        <item name="strokeWidth">0dp</item>
    </style>
    
    <!-- Input Field Style (applied to Card wrapping the edit text or TIL) -->
    <style name="Widget.App.InputContainer" parent="Widget.Material3.CardView.Elevated">
        <item name="cardBackgroundColor">@color/white</item>
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">2dp</item>
        <item name="strokeColor">@color/app_secondary</item>
        <item name="strokeWidth">1dp</item>
    </style>

    <style name="ShapeAppearance.App.Circle" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <style name="ShapeAppearance.App.RoundedSquare" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">12dp</item>
    </style>

    <style name="Theme.AppCleanHouse" parent="Base.Theme.AppCleanHouse" />
    <style name="Theme.AppCleanHouse.NoActionBar" parent="Base.Theme.AppCleanHouse" />
</resources>